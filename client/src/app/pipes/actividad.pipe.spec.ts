import { ActividadPipe } from './actividad.pipe';

describe('ActividadPipe', () => {
  it('create an instance', () => {
    const pipe = new ActividadPipe();
    expect(pipe).toBeTruthy();
  });
});
